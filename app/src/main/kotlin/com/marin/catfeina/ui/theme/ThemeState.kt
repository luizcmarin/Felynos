/*
 * Arquivo: com.marin.catfeina.ui.theme.ThemeState.kt
 * @project Catfeina
 * @description Define os estados de tema (claro, escuro, auto) aplicáveis.
 */
package com.marin.catfeina.ui.theme

/**
 * Representa o estado do tema (luminosidade) a ser aplicado.
 */
enum class ThemeState {
    CLARO,
    ESCURO
}